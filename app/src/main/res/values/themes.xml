<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Compose drives the actual Material 3 theming (see ui/theme).
        This base theme only needs to be a Material 3 parent so the Activity
        window has sane defaults before Compose takes over.
    -->
    <style name="Theme.AppBlocker" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
